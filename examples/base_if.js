/*
This file may have multiple example of short js
*/

var answer = "yes";
if (answer == "yes") {
    alert(answer)
}

var isMarried = true;
if (isMarried) {  // it is not necessary to state (isMarried = true)
    console.log(firstName + ' is married!');
} else {
    console.log(firstName + ' will hopefully marry soon');
}